import React from 'react';

import DefaultTemplate from '../../templates/DefaultTemplate';

export const CreateDataPage = (params) => {
  return (
    <DefaultTemplate>
      <div>Dashboard helll</div>
    </DefaultTemplate>
  );
}

export default CreateDataPage;
