import React from 'react';

import DefaultTemplate from '../../templates/DefaultTemplate';

export const EditTablePage = (params) => {
  return (
    <DefaultTemplate>
      <div>Dashboard helll</div>
    </DefaultTemplate>
  );
}

export default EditTablePage;
